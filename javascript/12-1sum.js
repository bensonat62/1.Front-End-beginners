// Calculate the sum from 1 to 10

let sum = 0;
for (let i = 1; i <= 10; i++) {
    sum += i;
}

// Find the result element and update its content
document.getElementById('result').innerText = `The sum from 1 to 10 is: ${sum}`;

